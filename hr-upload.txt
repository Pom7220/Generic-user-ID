<!DOCTYPE html>
<html lang="en">
<head>
<meta charset="UTF-8">
<meta name="viewport" content="width=device-width, initial-scale=1.0, viewport-fit=cover">
<meta name="apple-mobile-web-app-capable" content="yes">
<meta name="theme-color" content="#1C1C1E">
<title>HR Upload — Generic ID Manager</title>
<style>
*{box-sizing:border-box;margin:0;padding:0;-webkit-tap-highlight-color:transparent;-webkit-font-smoothing:antialiased}
:root{
  --blue:#0A84FF;--blue-lt:rgba(10,132,255,.14);--blue-dk:#0060DF;
  --green:#30D158;--green-lt:rgba(48,209,88,.14);--green-dk:#248A3D;
  --red:#FF453A;--red-lt:rgba(255,69,58,.14);--red-dk:#C0392B;
  --amber:#FF9F0A;--amber-lt:rgba(255,159,10,.14);--amber-dk:#B25000;
  --glass-bg:rgba(255,255,255,.72);--glass-border:rgba(255,255,255,.5);
  --glass-blur:saturate(180%) blur(24px);
  --bg:linear-gradient(160deg,#E8F4FF 0%,#F0EBF8 50%,#E6F7F0 100%);
  --surface2:rgba(255,255,255,.55);--text:#1C1C1E;--muted:#636366;--hint:#AEAEB2;
  --border:rgba(0,0,0,.09);--border2:rgba(0,0,0,.05);
  --radius:14px;--radius-lg:20px;--radius-xl:28px;
  --safe-top:env(safe-area-inset-top);--safe-bottom:env(safe-area-inset-bottom);
}
@media(prefers-color-scheme:dark){
  :root{
    --glass-bg:rgba(28,28,30,.78);--glass-border:rgba(255,255,255,.12);
    --bg:linear-gradient(160deg,#0D1B2A 0%,#1A0D2E 50%,#0A1F17 100%);
    --surface2:rgba(58,58,60,.7);--text:#F2F2F7;--muted:#AEAEB2;--hint:#636366;
    --border:rgba(255,255,255,.1);--border2:rgba(255,255,255,.06);
    --blue-lt:rgba(10,132,255,.2);--green-lt:rgba(48,209,88,.2);--red-lt:rgba(255,69,58,.2);--amber-lt:rgba(255,159,10,.2);
  }
}
html{height:100%;background:var(--bg)}
body{min-height:100vh;background:var(--bg);color:var(--text);font-family:-apple-system,BlinkMacSystemFont,'SF Pro Display','Segoe UI',Arial,sans-serif;padding-bottom:40px}

.topnav{position:sticky;top:0;z-index:100;padding:calc(var(--safe-top)+12px) 16px 12px;background:var(--glass-bg);-webkit-backdrop-filter:var(--glass-blur);backdrop-filter:var(--glass-blur);border-bottom:0.5px solid var(--glass-border)}
.topnav-inner{display:flex;align-items:center;gap:12px}
.nav-logo{width:36px;height:36px;border-radius:10px;background:linear-gradient(135deg,#FF9F0A,#FF6B35);color:#fff;display:flex;align-items:center;justify-content:center;font-size:12px;font-weight:800;flex-shrink:0}
.nav-title{font-size:17px;font-weight:700}
.nav-back{padding:6px 12px;border-radius:var(--radius);border:0.5px solid var(--glass-border);background:var(--surface2);color:var(--text);font-size:13px;font-weight:500;cursor:pointer;font-family:inherit;margin-left:auto}

.page{max-width:720px;margin:0 auto;padding:20px 16px}

.gcard{border-radius:var(--radius-lg);background:var(--glass-bg);-webkit-backdrop-filter:var(--glass-blur);backdrop-filter:var(--glass-blur);border:0.5px solid var(--glass-border);box-shadow:0 8px 32px rgba(0,0,0,.12);overflow:hidden;margin-bottom:16px}

.pdpa-banner{background:linear-gradient(135deg,var(--blue-lt),rgba(48,209,88,.08));border:0.5px solid rgba(10,132,255,.25);border-radius:var(--radius-lg);padding:16px;margin-bottom:16px}
.pdpa-banner h3{font-size:14px;font-weight:700;color:var(--blue-dk);margin-bottom:8px}
.pdpa-banner ul{font-size:13px;color:var(--muted);list-style:none;display:flex;flex-direction:column;gap:5px}
.pdpa-banner li::before{content:'🔒 ';margin-right:4px}

.drop-zone{border:2px dashed var(--border);border-radius:var(--radius-lg);padding:40px 20px;text-align:center;cursor:pointer;transition:all .2s;background:var(--surface2)}
.drop-zone.drag-over{border-color:var(--blue);background:var(--blue-lt)}
.drop-zone-icon{font-size:48px;margin-bottom:12px}
.drop-zone-title{font-size:17px;font-weight:600;margin-bottom:6px}
.drop-zone-sub{font-size:13px;color:var(--muted)}

.section-title{font-size:13px;font-weight:600;color:var(--muted);text-transform:uppercase;letter-spacing:.06em;margin-bottom:10px}

.diff-table{width:100%;border-collapse:collapse;font-size:13px}
.diff-table th{padding:10px 12px;text-align:left;font-size:11px;font-weight:600;color:var(--muted);text-transform:uppercase;letter-spacing:.05em;border-bottom:0.5px solid var(--border)}
.diff-table td{padding:10px 12px;border-bottom:0.5px solid var(--border2)}
.diff-new{background:var(--green-lt)}
.diff-resign{background:var(--red-lt)}
.diff-update{background:var(--amber-lt)}
.diff-badge{font-size:11px;font-weight:600;padding:2px 7px;border-radius:999px}
.diff-badge.new{background:var(--green-lt);color:var(--green-dk)}
.diff-badge.resign{background:var(--red-lt);color:var(--red-dk)}
.diff-badge.update{background:var(--amber-lt);color:var(--amber-dk)}
.diff-badge.nochange{background:var(--surface2);color:var(--muted)}

.btn{padding:10px 20px;border-radius:var(--radius);border:none;font-size:15px;font-weight:600;cursor:pointer;font-family:inherit;transition:opacity .15s,transform .1s;display:inline-flex;align-items:center;gap:7px}
.btn:active{transform:scale(.97)}
.btn:disabled{opacity:.45;cursor:not-allowed}
.btn-green{background:var(--green-dk);color:#fff}
.btn-glass{background:var(--surface2);color:var(--text);border:0.5px solid var(--glass-border)}
.btn-full{width:100%;justify-content:center}

.progress-bar{height:6px;border-radius:3px;background:var(--surface2);overflow:hidden;margin-top:8px}
.progress-fill{height:100%;border-radius:3px;background:var(--green-dk);transition:width .3s ease}

.spinner{display:inline-block;width:18px;height:18px;border:2.5px solid rgba(255,255,255,.3);border-top-color:#fff;border-radius:50%;animation:spin .6s linear infinite}
@keyframes spin{to{transform:rotate(360deg)}}
.hidden{display:none!important}

.stat-row{display:flex;gap:10px;margin-bottom:16px;flex-wrap:wrap}
.stat-chip{padding:8px 14px;border-radius:999px;font-size:13px;font-weight:600}
.stat-chip.new{background:var(--green-lt);color:var(--green-dk)}
.stat-chip.resign{background:var(--red-lt);color:var(--red-dk)}
.stat-chip.update{background:var(--amber-lt);color:var(--amber-dk)}
.stat-chip.total{background:var(--blue-lt);color:var(--blue-dk)}
</style>
</head>
<body>

<nav class="topnav">
  <div class="topnav-inner">
    <div class="nav-logo">HR</div>
    <div class="nav-title">HR Batch Upload</div>
    <button class="nav-back" onclick="location.href='bu.html'">← BU Dashboard</button>
  </div>
</nav>

<div class="page">

  <!-- PDPA Notice -->
  <div class="pdpa-banner">
    <h3>🔒 PDPA/GDPR Compliance</h3>
    <ul>
      <li>Only emp_code, bu_code, store_code, supervisor_code, status and dates are stored</li>
      <li>Full name, mobile, address and other PII in the CSV are discarded immediately after processing</li>
      <li>PII is fetched from SuccessFactors in real-time when needed for display</li>
      <li>This upload log retains only: emp_code, change type, and timestamp</li>
    </ul>
  </div>

  <!-- CSV Format Guide -->
  <div class="gcard" style="padding:16px;margin-bottom:16px">
    <div class="section-title">Required CSV format</div>
    <div style="background:var(--surface2);border-radius:var(--radius);padding:12px;font-family:monospace;font-size:12px;overflow-x:auto;white-space:nowrap;color:var(--muted)">
      emp_code, full_name*, bu_code, store_code, supervisor_emp_code, status, effective_date<br>
      EMP-5001, *, BKK, B12, EMP-4200, active, 2026-03-01<br>
      EMP-4421, *, BKK, B12, EMP-4200, resigned, 2026-03-15
    </div>
    <p style="font-size:12px;color:var(--hint);margin-top:8px">* full_name column is accepted but immediately discarded — not stored in database.</p>
  </div>

  <!-- Drop Zone -->
  <div class="gcard" style="padding:16px;margin-bottom:16px" id="upload-section">
    <div class="section-title">Upload CSV file</div>
    <div class="drop-zone" id="drop-zone" onclick="document.getElementById('file-input').click()">
      <div class="drop-zone-icon">📂</div>
      <div class="drop-zone-title">Drop CSV file here or click to browse</div>
      <div class="drop-zone-sub">Accepts .csv files · Max 5,000 rows per upload</div>
    </div>
    <input type="file" id="file-input" accept=".csv" class="hidden" onchange="handleFile(this.files[0])">
  </div>

  <!-- Diff Preview -->
  <div id="diff-section" class="hidden">
    <div class="gcard" style="padding:16px;margin-bottom:16px">
      <div class="section-title">Changes preview</div>
      <div class="stat-row" id="stat-row"></div>
      <div style="overflow-x:auto">
        <table class="diff-table">
          <thead>
            <tr>
              <th>Change</th>
              <th>Emp code</th>
              <th>BU</th>
              <th>Store</th>
              <th>Status</th>
              <th>Effective</th>
            </tr>
          </thead>
          <tbody id="diff-tbody"></tbody>
        </table>
      </div>
    </div>

    <div id="progress-section" class="hidden" style="margin-bottom:16px">
      <div style="font-size:14px;font-weight:600" id="progress-label">Processing…</div>
      <div class="progress-bar"><div class="progress-fill" id="progress-fill" style="width:0%"></div></div>
    </div>

    <div style="display:flex;gap:10px">
      <button class="btn btn-glass btn-full" onclick="resetUpload()">Cancel</button>
      <button class="btn btn-green btn-full" id="apply-btn" onclick="applyChanges()">Apply <span id="apply-count">0</span> changes</button>
    </div>
  </div>

  <!-- Result -->
  <div id="result-section" class="hidden">
    <div class="gcard" style="padding:24px;text-align:center">
      <div style="font-size:48px;margin-bottom:12px" id="result-icon">✅</div>
      <div style="font-size:20px;font-weight:700;margin-bottom:8px" id="result-title">Upload complete</div>
      <div style="font-size:14px;color:var(--muted)" id="result-desc"></div>
      <button class="btn btn-glass" style="margin-top:20px" onclick="resetUpload()">Upload another file</button>
    </div>
  </div>

</div>

<script src="https://cdn.jsdelivr.net/npm/@supabase/supabase-js@2/dist/umd/supabase.min.js"></script>
<script>
// ── CONFIG ──────────────────────────────────────────────────────
const SUPABASE_URL  = 'https://hnvytilmxoiaqceseooh.supabase.co';
const SUPABASE_ANON = 'eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.eyJpc3MiOiJzdXBhYmFzZSIsInJlZiI6Imhudnl0aWxteG9pYXFjZXNlb29oIiwicm9sZSI6ImFub24iLCJpYXQiOjE3NzM5MTc0ODIsImV4cCI6MjA4OTQ5MzQ4Mn0.x7WCLXRZEoorxEIuk5dwHG8EpasBGGB7peBysWUdElw';
// ────────────────────────────────────────────────────────────────

const sb = supabase.createClient(SUPABASE_URL, SUPABASE_ANON);
let SESSION=null, BUS={}, STORES={}, PARSED=[], DIFF=[];

async function init(){
  const {data:{session}}=await sb.auth.getSession();
  if(!session){window.location.href='index.html';return;}
  SESSION=session;

  // Load BUs and stores for lookup
  const {data:bus}=await sb.from('business_units').select('id,code,name');
  const {data:stores}=await sb.from('stores').select('id,code,bu_id');
  (bus||[]).forEach(b=>BUS[b.code]=b);
  (stores||[]).forEach(s=>STORES[s.code]=s);

  // Drag and drop
  const dz=document.getElementById('drop-zone');
  dz.addEventListener('dragover',e=>{e.preventDefault();dz.classList.add('drag-over')});
  dz.addEventListener('dragleave',()=>dz.classList.remove('drag-over'));
  dz.addEventListener('drop',e=>{e.preventDefault();dz.classList.remove('drag-over');if(e.dataTransfer.files[0])handleFile(e.dataTransfer.files[0])});
}

function handleFile(file){
  if(!file||!file.name.endsWith('.csv')){alert('Please upload a .csv file');return;}
  const reader=new FileReader();
  reader.onload=e=>parseCSV(e.target.result);
  reader.readAsText(file);
}

async function parseCSV(text){
  const lines=text.trim().split('\n');
  if(lines.length<2){alert('CSV must have a header row and at least one data row');return;}

  const headers=lines[0].split(',').map(h=>h.trim().toLowerCase().replace(/[^a-z_]/g,''));
  const col=name=>headers.indexOf(name);

  PARSED=lines.slice(1).map(line=>{
    const vals=line.split(',').map(v=>v.trim().replace(/^["']|["']$/g,''));
    return {
      emp_code: (vals[col('emp_code')]||'').toUpperCase(),
      // full_name intentionally excluded — PDPA: we read it but never store it
      bu_code: (vals[col('bu_code')]||'').toUpperCase(),
      store_code: (vals[col('store_code')]||'').toUpperCase(),
      supervisor_emp_code: (vals[col('supervisor_emp_code')]||'').toUpperCase()||null,
      status: (vals[col('status')]||'active').toLowerCase(),
      effective_date: vals[col('effective_date')]||new Date().toISOString().slice(0,10),
    };
  }).filter(r=>r.emp_code);

  if(!PARSED.length){alert('No valid rows found in CSV');return;}

  // Load existing employees
  const empCodes=PARSED.map(r=>r.emp_code);
  const {data:existing}=await sb.from('employees').select('emp_code,bu_id,store_id,status,supervisor_emp_code').in('emp_code',empCodes);
  const existMap={};(existing||[]).forEach(e=>existMap[e.emp_code]=e);

  // Build diff
  DIFF=PARSED.map(row=>{
    const ex=existMap[row.emp_code];
    const bu=BUS[row.bu_code];
    const store=STORES[row.store_code];
    let changeType='nochange';
    if(!ex) changeType='new';
    else if(ex.status!=='resigned'&&row.status==='resigned') changeType='resign';
    else if(ex.status!==row.status||ex.supervisor_emp_code!==row.supervisor_emp_code) changeType='update';
    return {...row,changeType,bu_id:bu?.id||null,store_id:store?.id||null,existing:ex};
  }).filter(r=>r.changeType!=='nochange');

  renderDiff();
}

function renderDiff(){
  const counts={new:0,resign:0,update:0};
  DIFF.forEach(r=>counts[r.changeType]=(counts[r.changeType]||0)+1);
  const total=DIFF.length;

  document.getElementById('stat-row').innerHTML=`
    <span class="stat-chip total">${total} total changes</span>
    ${counts.new?`<span class="stat-chip new">+${counts.new} new</span>`:''}
    ${counts.resign?`<span class="stat-chip resign">${counts.resign} resign</span>`:''}
    ${counts.update?`<span class="stat-chip update">${counts.update} updates</span>`:''}`;

  document.getElementById('apply-count').textContent=total;

  document.getElementById('diff-tbody').innerHTML=DIFF.map(r=>{
    const rowCls=r.changeType==='new'?'diff-new':r.changeType==='resign'?'diff-resign':r.changeType==='update'?'diff-update':'';
    return `<tr class="${rowCls}">
      <td><span class="diff-badge ${r.changeType}">${r.changeType}</span></td>
      <td style="font-family:monospace;font-weight:600">${r.emp_code}</td>
      <td>${r.bu_code}</td>
      <td>${r.store_code}</td>
      <td>${r.status}</td>
      <td>${r.effective_date}</td>
    </tr>`;
  }).join('');

  document.getElementById('upload-section').classList.add('hidden');
  document.getElementById('diff-section').classList.remove('hidden');
}

async function applyChanges(){
  if(!DIFF.length) return;
  const btn=document.getElementById('apply-btn');
  btn.disabled=true;btn.innerHTML='<span class="spinner"></span> Applying…';
  document.getElementById('progress-section').classList.remove('hidden');

  let done=0,errors=0;
  const total=DIFF.length;
  const bu_id=sessionStorage.getItem('gid_bu_id');

  for(const row of DIFF){
    try{
      const payload={
        emp_code:row.emp_code,
        bu_id:row.bu_id||bu_id,
        store_id:row.store_id||null,
        supervisor_emp_code:row.supervisor_emp_code||null,
        status:row.status,
        source:'hr_upload',
        updated_at:new Date().toISOString(),
      };
      if(row.status==='resigned') payload.resigned_at=row.effective_date;

      if(row.changeType==='new'){
        await sb.from('employees').upsert(payload,{onConflict:'emp_code'});
      } else {
        await sb.from('employees').update(payload).eq('emp_code',row.emp_code);
      }

      // Auto-disable generic IDs for resigned employees
      if(row.status==='resigned'&&row.existing){
        const {data:held}=await sb.from('generic_id_assignments')
          .select('id,app_id,store_id,generic_id')
          .eq('emp_code',row.emp_code)
          .neq('status','disabled');
        if(held?.length){
          await sb.from('generic_id_assignments')
            .update({status:'disabled',disabled_at:new Date().toISOString(),disabled_reason:'Employee resigned via HR upload'})
            .eq('emp_code',row.emp_code).neq('status','disabled');
          for(const a of held){
            await sb.from('audit_log').insert({
              store_id:a.store_id,app_id:a.app_id,
              generic_id:a.generic_id,emp_code:row.emp_code,
              event_type:'resign_auto_disable',event_by:'hr_upload',
              detail:{effective_date:row.effective_date,source:'csv_upload'}
            });
          }
        }
      }

      // Write upload log — PDPA: only emp_code and change type, no PII
      await sb.from('hr_upload_log').upsert({
        emp_code:row.emp_code,
        change_type:row.changeType,
        uploaded_by:SESSION.user.email,
        effective_date:row.effective_date,
        uploaded_at:new Date().toISOString()
      },{onConflict:'emp_code'});

      done++;
    }catch(e){
      console.error('Row error:',row.emp_code,e);
      errors++;
    }

    const pct=Math.round((done+errors)/total*100);
    document.getElementById('progress-fill').style.width=pct+'%';
    document.getElementById('progress-label').textContent=`Processing ${done+errors} of ${total}…`;
  }

  showResult(done,errors,total);
}

function showResult(done,errors,total){
  const hasErrors=errors>0;
  document.getElementById('result-icon').textContent=hasErrors?'⚠️':'✅';
  document.getElementById('result-title').textContent=hasErrors?'Upload completed with errors':'Upload complete';
  document.getElementById('result-desc').textContent=
    `${done} records processed successfully.${errors?' '+errors+' records had errors.':''} `+
    `Resigned employees have had their generic IDs automatically disabled.`;
  document.getElementById('diff-section').classList.add('hidden');
  document.getElementById('result-section').classList.remove('hidden');
}

function resetUpload(){
  PARSED=[];DIFF=[];
  document.getElementById('upload-section').classList.remove('hidden');
  document.getElementById('diff-section').classList.add('hidden');
  document.getElementById('result-section').classList.add('hidden');
  document.getElementById('progress-section').classList.add('hidden');
  document.getElementById('progress-fill').style.width='0%';
  document.getElementById('file-input').value='';
  const btn=document.getElementById('apply-btn');
  btn.disabled=false;btn.innerHTML='Apply <span id="apply-count">0</span> changes';
}

init();
</script>
</body>
</html>
